import React, { useEffect, useState } from "react";
import { useSelector, } from "react-redux";

const Card = (state) => {

    return (
        <div>
<h3> Cards</h3>
            {/* {cardsArr.map(card => (
                <p>{card.name}</p>

           ))} */}

        </div>

    )
} 

export default Card